import colors from './colors'
// import validator from './validators'
import shadow from './shadow'
import { scale, verticalScale, device, wp, hp } from './scale'
export { colors, verticalScale, scale, device, shadow, wp, hp }