import { Text, View } from "react-native";
import React, { Component } from "react";
import Viewer from "../screensWeb/Viewer";

export default class DrawerItems extends Component {
  render() {
    return (
      <View>
        <Viewer />
      </View>
    );
  }
}
